<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">
    <!-- 회원가입 -->
    <insert id="insertMember" parameterType="com.flo.alwaysbom.member.vo.MemberVO">
        INSERT INTO MEMBER (ID, PW, NAME, BIRTH, GENDER, PHONE)
        VALUES (#{id}, #{pw}, #{name}, #{birth}, #{gender}, #{phone})
    </insert>

    <!-- 로그인 -->
    <select id="Login" parameterType="com.flo.alwaysbom.member.vo.MemberVO" resultType="Int">
        SELECT COUNT(*)
        FROM MEMBER
        WHERE ID = #{id}
          AND PW = #{pw}
    </select>
</mapper>
