<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="question">

    <insert id="addque" parameterType="com.flo.alwaysbom.community.question.vo.QuestionVo">
        INSERT INTO MYQ(NAME, CONTENT, IMAGE, MEMBER_ID, EMAIL_SEND, ANSWER)
        VALUES (#{name}, #{content}, #{image}, "motherfathergnetle", #{emailSend}, #{answer})
    </insert>

</mapper>